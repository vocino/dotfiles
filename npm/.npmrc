# Global npm configuration
# This file is symlinked from ~/dotfiles/npm/.npmrc

# Default registry (uncomment and modify if using a private registry)
# registry=https://registry.npmjs.org/

# Save exact version (not ^ or ~)
# save-exact=true

# Use npm audit by default
audit=true

# Set fund messages to false (less noise)
fund=false

# Progress bar settings
progress=true

# Logging level
loglevel=warn

# Prefix for global packages (optional)
# prefix=~/.npm-global
